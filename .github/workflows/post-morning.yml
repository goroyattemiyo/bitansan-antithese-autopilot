name: Post Morning (Legacy Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'This legacy workflow is disabled.'
          echo 'Use Post Scheduler. Manual runs default to dry_run=true.'
